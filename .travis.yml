language: common-lisp
sudo: required

env:
  matrix:
    - LISP=sbcl
    - LISP=ccl
# by some reason 32bit versions lacks necessary libraries on TravisCI    
#    - LISP=ccl32

os:
  - linux
  - osx

install:
  # Install cl-travis
  - curl https://raw.githubusercontent.com/luismbo/cl-travis/master/install.sh | bash

script:
  - cl -l prove -e '(or (prove:run :pypath-test) (uiop:quit -1))'

